// console.log(window)
// browser global object is window
// but not for node environment
// node env->global obje is blobal

// global Object=> In browser it is called window but in node env it is called global
// globalThis =>global object of the node env

// console.log(global);

console.log(globalThis);

// this keyword =>{}(empty object) in node env,window object in browser
console.log(this);